# dev | prod | test
NODE_ENV=dev
PORT=3000

# Vergul bilan ajratilgan originlar, yoki * (hammasiga ruxsat).
# Prod: CORS_ORIGIN=https://example.com,https://app.example.com
CORS_ORIGIN=*

DB_HOST=localhost
DB_PORT=5432
DB_USER=
# Ixtiyoriy — parolsiz lokal Postgres uchun bo'sh qoldiring.
DB_PASSWORD=
DB_NAME=
